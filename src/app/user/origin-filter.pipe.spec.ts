import { OriginFilterPipe } from './origin-filter.pipe';

describe('OriginFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OriginFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
